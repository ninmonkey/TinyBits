{
    /*
        about: Snippets applied to all file types
    */

    "Insert Date": {
        "prefix": "date",
        "body": [
            "${CURRENT_YEAR}-${CURRENT_MONTH}-${CURRENT_DATE}"
        ],
        "description": "Insert current date in 'yyyy-MM-dd' format | 🦍"
    },
    "Selection To File Uri for Markdown": {
        /*
            Escapes filepaths making them directly clickable
            In markdown / vscode or the console, use: <ctrl + LMB>

            Input is either:
                1)  selected text
                2)  else your clipboard

            For example this:
                "c:\foo bar\stuff"

            Is converted to:
                [c:\foo bar\stuff](file:///c:\foo%20bar\stuff)
		*/
        "prefix": "file uri",
        "body": [
            // ref: "${TM_FILENAME/[\\.-]/_/g}"
            // "[${1:${TM_SELECTED_TEXT}}](file:///${CLIPBOARD})$0"
            "[${1:${TM_SELECTED_TEXT}}](file:///${CLIPBOARD/[\\ ]/%20/g})$0"
        ],
        "description": "Converts Selection (or clipboard) to a file:/// uri and escapes spaces making them <ctrl+LMB> clickable in markdown/github/localhost/console/ markdown links"
    },
}